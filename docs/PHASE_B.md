# Phase B — 表示のみ

- テーマ詳細: 既存 `heat.breadth` の now_above / past_above / eligible_n を割合として表示。凍結プロファイルの50日移動平均・10営業日前比較と一致することをテストする。
- 初動: 既存 `turning_watch.active` の集合を維持。理由、勢いの確認状態、市場ストレス注意、Breadthを表示。新しい初動条件やランキングは作らない。
- 銘柄詳細: Phase A の1/5/21/63営業日価格リターン、RVOL、基準日を再利用。追加APIなし。欠損・unresolvedは「—」。テーマ出力と価格データの日付を混同しない。

## 互換性

データ更新、凍結ファイル、会計、判断enum、投資根拠、ティッカー検証、pager、ナビ、既存CSS、Forwardは変更しない。
Observationの既存テーマ出力にBreadth・反転理由は既に含まれる。Phase Aの価格指標もそのまま再利用するため台帳・スキーマ・保存内容への変更は不要。時点不一致のunresolvedは維持する。
正式factor採用・売買接続・自動昇格・モデル変更は行わない。

## 確認箇所

iPhone: テーマ詳細の「上昇の広がり」、テーマの初動タブ、銘柄詳細の補助指標。375〜430pxを想定し、補助指標は2列、Breadthは3列で横幅を固定しない。CSSは追加セクションのみに適用。
Phase BのiPhone実機確認はユーザー確認待ち。既存Scroll Snapのソースは変更しない。
