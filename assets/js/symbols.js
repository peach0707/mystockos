import {esc} from './ui.js';
export const normalizeTicker=value=>String(value??'').trim().toUpperCase();
let master=[];export let masterStatus='銘柄一覧を読み込んでいます…';
export let symbolSourcesComplete=false;
let automaticQuotes={};
export function setQuoteCoverage(rows){automaticQuotes=rows||{};}
export function quoteCoverageLabel(symbol){const row=automaticQuotes[symbol];return row?Number.isFinite(row.price)?'価格自動更新':'自動取得対象・取得待ち':'価格は自動取得の対象外';}
export function validateMaster(d){if(d?.schema_version!==1||!Array.isArray(d.symbols)||!d.symbols.length||d.symbols.some(r=>!r.id||!r.symbol||!r.name||!r.exchange||normalizeTicker(r.symbol)!==r.symbol)||new Set(d.symbols.map(r=>r.id)).size!==d.symbols.length)throw Error('銘柄一覧の形式が不正です');return d.symbols;}
export function searchSymbols(rows,query){const q=normalizeTicker(query);if(!q)return [];return rows.filter(r=>r.symbol.includes(q)||r.name.toUpperCase().includes(q)).sort((a,b)=>(a.symbol===q?0:a.symbol.startsWith(q)?1:2)-(b.symbol===q?0:b.symbol.startsWith(q)?1:2)||a.symbol.localeCompare(b.symbol)||a.exchange.localeCompare(b.exchange)).slice(0,12);}
export function selectedSymbol(rows,query,id){const r=rows.find(r=>r.id===id&&r.symbol===normalizeTicker(query));if(!r)throw Error('該当する銘柄が見つかりません。候補から銘柄と市場を選択してください。');return r;}
export function ensureUnique(symbol,existing){if(existing.some(x=>normalizeTicker(x)===normalizeTicker(symbol)))throw Error(`${normalizeTicker(symbol)}は登録済みです。`);}
export const registerWatch=(s,r)=>{ensureUnique(r.symbol,s.watch);s.watch.push(r.symbol);remember(s,r);};
export const remember=(s,r)=>{s.securities??={};if(s.securities[r.symbol]&&s.securities[r.symbol].id!==r.id)throw Error('同じティッカーが別市場で登録済みです。市場を確認してください。');s.securities[r.symbol]={...r};};
export async function loadSymbols(){
 const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),15000);
 try{
  const results=await Promise.allSettled(['symbols','etfs'].map(async name=>{
   const res=await fetch(new URL(`../../data/${name}.json`,import.meta.url),{cache:'no-store',signal:controller.signal});
   if(!res.ok)throw Error();const d=await res.json();return {name,rows:validateMaster(d),asOf:d.as_of};
  }));
  const loaded=results.filter(r=>r.status==='fulfilled').map(r=>r.value);
  symbolSourcesComplete=loaded.length===2;
  master=[...new Map(loaded.flatMap(d=>d.rows).map(r=>[r.id,r])).values()];
  masterStatus=master.length?`米国の株式・ETF ${master.length.toLocaleString('ja-JP')}件。${loaded.map(d=>d.asOf).sort()[0]} 時点。${loaded.length<2?'一部の銘柄一覧は取得待ち。':''}価格の自動取得対象とは異なります。`:'銘柄一覧を取得できません。新規登録を停止しています。設定から再読み込みしてください。';
 }finally{clearTimeout(timer);}return master;
}
export const listingFor=ticker=>master.find(r=>r.symbol===ticker);
export const isLeveraged=record=>!!record&&/\b[23]x\b|\bbull\b|\bbear\b|\bultra|\binverse\b/i.test(record.name);
export function picker(name='ticker',value='',multi=false,record=null){return `<div class="symbol-picker" data-symbol-picker="${name}" ${multi?'data-multi="true"':''}><label>${multi?'関連銘柄を検索・選択':'銘柄を検索・選択'}<input name="${multi?name+'_query':name}" data-symbol-query autocomplete="off" autocapitalize="characters" spellcheck="false" placeholder="例：NVD、NVIDIA" value="${esc(value)}" maxlength="80" ${multi?'':'required'} aria-label="銘柄名またはティッカーを検索"></label><input type="hidden" name="${multi?name:name+'_id'}" value="${multi?'[]':esc(record?.id||'')}"><div class="symbol-results" aria-live="polite">${record?`${esc(record.symbol)} · ${esc(record.name)} · ${esc(record.exchange)}`:'候補をタップして市場を選択してください。'}</div><div class="symbol-chips"></div><small class="master-status">${esc(masterStatus)}</small></div>`;}
export function fromForm(f,name='ticker'){return selectedSymbol(master,f.get(name),f.get(name+'_id'));}
export function manyFromForm(f,name='tickers'){const ids=JSON.parse(String(f.get(name)||'[]'));if(!Array.isArray(ids))throw Error('関連銘柄を選択してください。');return [...new Set(ids)].map(id=>{const r=master.find(x=>x.id===id);if(!r)throw Error('関連銘柄を確認できません。');return r;});}
export function verifyIncoming(next,current){const old=new Set([...current.watch,...current.holdings.map(h=>h.ticker),...current.dividends.map(d=>d.ticker),...current.news.flatMap(n=>n.tickers)]);for(const symbol of [...next.watch,...next.holdings.map(h=>h.ticker),...next.dividends.map(d=>d.ticker),...next.news.flatMap(n=>n.tickers)])if(!old.has(symbol)){const matches=master.filter(r=>r.symbol===symbol);const r=next.securities?.[symbol];if(!matches.length||matches.length>1&&!matches.some(x=>x.id===r?.id))throw Error(`${symbol}の銘柄・市場を確認できません。候補から選択してください。`);remember(next,r?selectedSymbol(master,symbol,r.id):matches[0]);}}
export function bindSymbolPickers(){document.addEventListener('input',event=>{const input=event.target.closest('[data-symbol-query]');if(!input)return;const root=input.closest('[data-symbol-picker]'),name=root.dataset.symbolPicker;if(!root.dataset.multi)root.querySelector(`[name="${name}_id"]`).value='';const matches=searchSymbols(master,input.value);root.querySelector('.symbol-results').innerHTML=matches.length?matches.map(r=>`<button type="button" data-symbol-id="${esc(r.id)}"><b>${esc(r.symbol)}</b><span>${esc(r.name)}</span><small>${esc(r.exchange)} ${esc(r.mic_code||'')} · ${esc(quoteCoverageLabel(r.symbol))}</small></button>`).join(''):esc(master.length?(input.value.trim()?'該当する銘柄が見つかりません':'銘柄名・ティッカーを入力してください'):masterStatus);});document.addEventListener('click',event=>{const button=event.target.closest('[data-symbol-id],[data-symbol-remove]');if(!button)return;const root=button.closest('[data-symbol-picker]'),name=root.dataset.symbolPicker;if(root.dataset.multi){const hidden=root.querySelector(`[name="${name}"]`);let ids=JSON.parse(hidden.value);if(button.dataset.symbolRemove)ids=ids.filter(id=>id!==button.dataset.symbolRemove);else{const r=master.find(x=>x.id===button.dataset.symbolId);if(!r)return;if(!ids.some(id=>master.find(x=>x.id===id)?.symbol===r.symbol))ids.push(r.id);}hidden.value=JSON.stringify(ids);root.querySelector('.symbol-chips').innerHTML=ids.map(id=>{const r=master.find(x=>x.id===id);return `<button type="button" data-symbol-remove="${esc(id)}">${esc(r.symbol)} · ${esc(r.exchange)} ×</button>`;}).join('');root.querySelector('[data-symbol-query]').value='';root.querySelector('.symbol-results').innerHTML='';}else{const r=master.find(x=>x.id===button.dataset.symbolId);if(!r)return;root.querySelector(`[name="${name}"]`).value=r.symbol;root.querySelector(`[name="${name}_id"]`).value=r.id;root.querySelector('.symbol-results').textContent=`選択済み：${r.name} · ${r.exchange}`;root.dispatchEvent(new CustomEvent('symbol-selected',{bubbles:true,detail:{symbol:r.symbol}}));}});}
