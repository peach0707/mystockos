import {usageError} from './usage.js';
import {esc} from './ui.js';
import {updateDetails} from './freshness.js';
export {homeView,stocksView} from './decision-ui.js';
export {newsView} from './news-ui.js';
export function settingsView(s,error,data={}){return `<h1>設定</h1>${updateDetails(data)}<section class="card"><h2>今日の方針</h2><form id="policy-form"><label>自分の確認事項<textarea name="policy" maxlength="3000">${esc(s.policy)}</textarea></label><button>保存</button></form></section><section class="card"><h2>データ連携・バックアップ</h2><p>個人データはこのブラウザに保存されます。端末間では自動同期されません。Safariのデータ削除に備え、バックアップを保存してください。</p>${error?`<p class="warning">${esc(error)}</p>`:''}<button id="export-private">バックアップを書き出す</button><h3>判断・投資根拠の利用履歴</h3>${usageError()?`<p class="warning">${esc(usageError())}</p>`:''}<p>利用した日と判断変更時に、この端末だけへ記録します。アプリを開かなかった日の履歴は作りません。会計バックアップとは別に保存してください。</p><button id="export-usage">利用履歴を書き出す</button><label>JSONバックアップを読み込む（現在の内容を置換）<input type="file" id="import-private" accept=".json,application/json"></label><label>日次評価CSVを読み込む（同日付の評価を更新）<input type="file" id="import-csv" accept=".csv,text/csv"></label><button class="subtle" id="csv-template">CSVテンプレート</button><p class="muted">評価CSV列：date, timestamp, assets_jpy, valuation_basis, status。時刻は空欄可（日本会計日の終了時点）。CSV更新では既存の寄与・配分明細を維持します。</p><label>外部入出金CSVを読み込む（同じIDは更新）<input type="file" id="import-flows-csv" accept=".csv,text/csv"></label><button class="subtle" id="flow-csv-template">入出金CSVテンプレート</button><p class="muted">入出金CSV：id,date,timestamp,amount_jpy,type,note。入金は正の額/deposit、出金は負の額/withdrawal。同じIDの再取込で二重登録しません。</p><a href="#portfolio/flows">入出金台帳・完全性 →</a><p>旧形式のJSONも読み込めます。CSV・JSONは送信せず、端末内で検証してから保存します。</p></section><section class="card"><h2>ホーム画面から起動</h2><p>Safariの共有から「ホーム画面に追加」を選び、「株ゴリラ🦍」として追加してください。</p><small>通信が必要です。オフライン用キャッシュは使用していません。</small><h2>表示について</h2><p>株ゴリラ🦍 · 2026.09.22 更新版</p><p>テーマ判定システム v1.0 の計算は維持しています。日常利用と検証用予測は分離しています。先行判定エンジンは実データを蓄積・検証中です。未検証スコアの売買判断への接続、モデル自動変更・自動昇格は行いません。</p><button id="refresh-data">公開データを再読み込み</button></section>`;}
